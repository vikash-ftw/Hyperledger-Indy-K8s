apiVersion: v1
kind: ConfigMap
metadata:
  name: indy-config
  namespace: indy
data:
  # 1. Main network configuration
  indy_config.py: |
    NETWORK_NAME = 'indy_prod'
    
  # 2. Pool Genesis File (Placeholder)
  pool_transactions_genesis: |
    # PLACEHOLDER: this will be generated by Job
    
  # 3. Domain Genesis File (Placeholder)
  domain_transactions_genesis: |
    # PLACEHOLDER: this will be generated by Job